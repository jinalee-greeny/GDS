# Foundations Token Studio — Figma Plugin

Generated by `python3 build_apps.py` (edit `core/*` and `plugin/*.template.*` / `plugin/code.src.js`, never the generated `code.js`/`ui.html`).

## Load in Figma (development)
1. `python3 build_apps.py` from the repo root.
2. Figma desktop → Menu → Plugins → Development → Import plugin from manifest…
3. Select `plugin/manifest.json`.
4. Run: Plugins → Development → Foundations Token Studio.

## Use
- Tweak tokens in the panel (same editor as the web app), now organized into 6 categories.
- Turn on what to export using the toggles inside each category (see next section).
- Click "Figma에 적용" (always visible at the bottom) → writes every checked item.
- Re-applying updates in place (no duplicates).

## Export toggles (inline)
Export options live inside the editor, not in a separate panel. Each category header has a master checkbox that turns the whole category on/off (and shows all/some/none); each editor module has its own toggle to its left — `Var` for variable groups, `Effect` for the shadow module (shadow is exported ONLY as an Effect Style, never a variable). Typography also has a composite **Text Styles** item at the bottom: check it to reveal the weight checkboxes + font-family input.

"Figma에 적용" (always visible at the bottom) writes the checked items. Failure reasons (e.g. an unavailable font) are listed in the bottom bar.

## Manual QA checklist
- [ ] Panel renders (controls + preview + WCAG) inside Figma.
- [ ] Apply with all categories → `Foundations` collection created with color/*, space/*, radius/*, etc. of correct types.
- [ ] Colors resolve to the expected swatches; spacing/radius are FLOATs; fontFamily/easing are STRINGs.
- [ ] Re-apply after changing a hue → variables update, count = updated (no new dupes).
- [ ] Deselect some categories/module toggles → only checked items written.
- [ ] Result summary shows 생성/갱신/실패 counts.

### Manual QA (Figma desktop)
- [ ] Six categories render, each header with a master export toggle + per-module toggles; the master reflects all/some/none.
- [ ] Apply with e.g. Color + Effects(Effect Styles) → Foundations color variables + shadow/* effect styles are actually created (async getter fix).
- [ ] Apply Typography with Text Styles + a real font → text/<size>/<weight> styles created; an unavailable font lists a clear failure in the bottom bar; other items still succeed.
- [ ] Re-apply → updates in place, no duplicates. (Deselecting an item does not delete previously-created variables/styles — orphans remain.)

## Note
- Changing a token's underlying variable TYPE (COLOR/FLOAT/STRING) causes the variable to be removed and recreated with a new ID, which can orphan any existing Figma layer/variable bindings to that token. This is expected Figma-API behavior, noted so users aren't surprised.
